apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: apps
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/instance: backend
data:
  S3_ENDPOINT_URL: "http://cluster.local"
