apiVersion: v1
kind: ConfigMap
metadata:
  name: frontend-config
  namespace: apps
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/instance: frontend
data:
  NODE_ENV: "production"
  VITE_API_URL: "/api/v1"