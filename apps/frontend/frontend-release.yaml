apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frontend
  namespace: apps
spec:
  interval: 10m
  releaseName: frontend
  chart:
    spec:
      chart: frontend
      version: "0.1.18"
      sourceRef:
        kind: HelmRepository
        name: helm-charts
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/reservoir-sandbox/frontend
      tag: "f32f619e8f531c6a56f13dc5eeaaba71fd901af9"
      pullPolicy: IfNotPresent
    service:
      type: ClusterIP
      port: 3000
